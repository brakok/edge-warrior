var Camera = function(x, y, resWidth, resHeight, vpWidth, vpHeight, zoom, speedX, speedY, speedZoom){	this.x = x;	this.y = y;		this.speed = {		x: speedX,		y: speedY,		zoom: speedZoom	};		this.focus = {		x: this.x,		y: this.y	};		this.resolution = {		width: resWidth,		height: resHeight	};		this.viewport = {		width: vpWidth,		height: vpHeight	};		this.zoom = zoom;		//Use targeted zoom to smoother zoom.	this.targetedZoom = zoom;		this.lookAt(x, y);};Camera.prototype.lookAt = function(x, y){	this.focus.x = x;	this.focus.y = y;};Camera.prototype.update = function(){	//Update zoom.	if(this.speed.zoom != null && this.zoom != this.targetedZoom)	{		var diff = this.targetedZoom -this.zoom;		var calculatedSpeedZoom = this.speed.zoom*diff*diff*diff;				if(Math.abs(this.zoom - this.targetedZoom) > Math.abs(calculatedSpeedZoom))			this.zoom += calculatedSpeedZoom;		else			this.zoom = this.targetedZoom;	}	if(this.speed.x == null && this.speed.y == null)	{		this.x = this.focus.x;		this.y = this.focus.y;				return;	}	//Translate X.	if(this.x != this.focus.x)	{		var diff = this.focus.x - this.x;		var calculatedSpeedX = this.speed.x*diff*diff*diff;				if(Math.abs(this.x - this.focus.x) > calculatedSpeedX)			this.x += calculatedSpeedX;		else			this.x = this.focus.x;	}		//Translate Y.	if(this.y != this.focus.y)	{		var diff = this.focus.y - this.y;		var calculatedSpeedY = this.speed.y*diff*diff*diff;				if(Math.abs(this.y - this.focus.y) > calculatedSpeedY)			this.y += calculatedSpeedY*(this.focus.y > this.y ? Constants.Camera.UPWARD_FACTOR : 1); //Upward factor slowdowns camera ascension.		else			this.y = this.focus.y;	}};Camera.prototype.project = function(sprite, x, y, scaleX, scaleY){		//Position.	var factorX = this.resolution.width/this.viewport.width;	var factorY = this.resolution.height/this.viewport.height;				x = (x*this.zoom - (this.x*this.zoom - this.viewport.width*0.5))*factorX;	y = (y*this.zoom - (this.y*this.zoom - this.viewport.height*0.5))*factorY;		sprite.setPosition(new cc.Point(x, y));		//Scale.		var tmpScaleX = factorX*this.zoom;	var tmpScaleY = factorY*this.zoom;		if(scaleX != null)		tmpScaleX *= scaleX;	if(scaleY != null)		tmpScaleY *= scaleY;		sprite.setScale(tmpScaleX, tmpScaleY);};